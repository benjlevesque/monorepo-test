version: 2

jobs:
  build-p1:
    docker:
      - image: "circleci/node:9.6.1"
    working_directory: ~/repo/packages/package1
    steps:
      - checkout:
          path: ~/repo
      - run: node index.js

workflows:
  version: 2.1
  build:
    jobs:
      - build-p1